
public class Conta {
	
	private static final double saldo = 0;

	public boolean saca(double valor) {
	    if(this.saldo >= valor) {
	        Conta.saldo -= valor;
	        return true;
	    } else {
	        return false;
	    }
	}

}
